{
  "name": "Text flow parity: browse + category + numeric selection",
  "description": "Tests that browse text, category name, and numeric input all trigger the text-only picker flow (numbered lists + typed numbers).",
  "phone": "+40710000003",
  "profileName": "Replay Template",
  "resetConversation": true,
  "steps": [
    {
      "body": "ce aveti?",
      "note": "Browse text — should send category picker as numbered list",
      "expectAsyncBodyIncludes": ["Categorii disponibile"],
      "pauseMs": 5000
    },
    {
      "body": "Dairy",
      "note": "Category name text — should send product list as numbered list",
      "expectAsyncBodyIncludes": ["Produse din"],
      "pauseMs": 5000
    },
    {
      "body": "1",
      "note": "Numeric input with active pending_selection (product_list) — should resolve to first product and ask qty",
      "expectAsyncBodyIncludes": ["cantitate"],
      "pauseMs": 5000
    }
  ]
}
